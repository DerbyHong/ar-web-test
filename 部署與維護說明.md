# WebAR 專案：本機維護與 GitHub Pages 部署指南

這份指南將引導您如何在本地管理 AR 項目素材、如何將更新推送到 GitHub Pages，以及萬一出現問題時如何利用 Git 進行版本回退與維護。

---

## 📂 專案核心結構說明
在開始之前，請先熟悉目前的專案資料結構：
* **`docs/`**：這是您整個網站的**前端靜態目錄**（即 GitHub Pages 讀取的目錄）。
  * `docs/index.html`、`main.js`：前台首頁。
  * `docs/ar.html`、`ar.js`：AR 掃描與影像追蹤頁。
  * `docs/admin.html`、`admin.js`：後台管理介面。
  * `docs/data/items.json`：**AR 項目資料庫**。您的新增、下架、排序等狀態皆儲存在此。
  * `docs/uploads/`：存放所有上傳的印刷圖片、MP4 影片與 `.mind` 辨識檔。
* **`server.js`**：這是在您**本機電腦**上執行的 Node.js 後端伺服器，負責處理後台登入驗證、檔案寫入與 API 服務。

---

## 一、修改後台資料（新增/下架/刪除 AR 項目）的方法

由於 GitHub Pages 只支援純靜態網頁（無後端伺服器），您**無法**直接在線上的後台網址進行登入或上傳素材。所有的素材管理都必須在您的**本機電腦**上完成。

### 本地操作步驟：
1. **啟動本機伺服器**：
   在專案根目錄（`d:\碩士\AR探索網頁`）開啟終端機（如 PowerShell、CMD 或 Git Bash），執行以下指令：
   ```bash
   node server.js
   ```
   *當看到 `AR web app is running at http://127.0.0.1:4173` 提示，代表伺服器已順利啟動。*

2. **開啟後台管理介面**：
   在瀏覽器中開啟：[http://localhost:4173/admin.html](http://localhost:4173/admin.html)

3. **登入後台**：
   在登入欄位中輸入密碼：**`admin123`**（此為預設密碼，若要發布上線，可透過環境變數 `ADMIN_PASSWORD` 來修改）。

4. **管理 AR 素材**：
   * **新增項目**：填寫圖片名稱，選擇 JPG/PNG 圖片與 MP4 影片。上傳後，前端會**自動編譯並產生 `.mind` 辨識檔**（若瀏覽器因安全阻擋編譯，可先使用備援欄位手動上傳由 MindAR 官網工具產生的 `.mind` 檔）。點擊「新增項目」後即儲存。
   * **發布 / 下架**：在右側列表點擊「發布」或「下架」按鈕。未發布的項目在前台將不會顯示。
   * **測試掃描**：點擊「測試掃描」會在新分頁開啟本地 AR 畫面，可用手機或電腦鏡頭對準印刷品實體進行辨識度與影片貼合測試。
   * **刪除項目**：點擊「刪除」按鈕，系統會將該項目從資料庫移除，並**自動同步刪除** `docs/uploads/` 底下對應的實體圖片、影片與辨識檔，維持資料夾乾淨。

5. **關閉本機伺服器**：
   在終端機中按下 **`Ctrl + C`** 即可安全關閉本地伺服器。

---

## 二、將專案推送到 GitHub 更新線上網頁的方法

當您在本機後台完成更新後，變更會即時寫入到本地的 `docs/data/items.json` 與 `docs/uploads/` 內。若要將這些變更發布到線上，請將專案推送到 GitHub。

### 線上更新步驟：
在專案根目錄（`d:\碩士\AR探索網頁`）打開終端機，依序執行以下 Git 指令：

1. **查看變更狀態**（非必要，但建議執行以確認檔案正確性）：
   ```bash
   git status
   ```
   *此時會看到 `docs/data/items.json` 顯示為已修改（modified），且 `docs/uploads/` 下有新上傳的媒體檔案。*

2. **將所有變更加入 Git 暫存區**：
   ```bash
   git add .
   ```

3. **提交更新並填寫說明**：
   ```bash
   git commit -m "feat: 更新 AR 項目素材 - <寫下您更新了什麼，例如：新增用餐區圖片>"
   ```

4. **推送至 GitHub 線上倉庫**：
   ```bash
   git push origin main
   ```

5. **線上生效驗證**：
   推送完成後，等待約 1 ~ 2 分鐘讓 GitHub Pages 完成靜態建置。接著使用手機開啟您的 GitHub Pages 網址並重新整理，即可看到最新的 AR 辨識圖片與影片！

---

## 三、萬一出現問題，退回到指定版本的方法

如果在開發或更新過程中不小心改壞了程式碼、上傳了錯誤的檔案，或者想回退到之前的某個穩定版本，可以使用 Git 的版本控制功能來安全回退。

### 情況 A：丟棄目前所有「尚未 Commit」的修改（回歸上一次 Commit 的乾淨狀態）
如果您剛做了一些測試或修改，還沒有執行 commit，但程式碼突然壞掉想完全重來：
```bash
# ⚠️ 注意：這會清除所有尚未 commit 的本地修改，執行前請確認不需要保留這些變更
git reset --hard HEAD
```

### 情況 B：查詢歷史版本並「回退到指定的 Commit 歷史節點」
如果您想將專案完全恢復到過去的某個特定的歷史版本：

1. **查詢歷史 Commit 紀錄（獲取 Commit ID）**：
   ```bash
   git log --oneline -n 10
   ```
   這會列出最近的 10 次提交，格式如下：
   ```text
   a1b2c3d (HEAD -> main, origin/main) feat: 更新 AR 項目素材
   e5f6g7h chore: 將 public 資料夾重命名為 docs 以相容 GitHub Pages
   9z8y7x6 feat: 靜態化改造與路徑相對化以相容 GitHub Pages
   ```
   *每行開頭的 7 碼英文與數字（如 `e5f6g7h`）就是該版本的 **`Commit ID`**。*

2. **安全回退（推薦）- 建立新分支來查看/修改舊版本**
   如果您只是想回去看看舊版程式碼，或在舊版基礎上做修改，而不想破壞目前的 `main` 分支：
   ```bash
   # 以 e5f6g7h 版本為基礎，建立並切換至一個名為 backup-version 的新分支
   git checkout -b backup-version e5f6g7h
   ```

3. **強制回退（謹慎使用）- 將目前分支「徹底重設」到指定版本**
   如果您非常確定目前的最新版完全不要了，要強制把目前的本地 `main` 分支退回到歷史版本（如 `e5f6g7h`）：
   ```bash
   # 強制將本地的 main 分支重設為 e5f6g7h 版本
   git reset --hard e5f6g7h
   ```
   *⚠️ 注意：這會抹除 `e5f6g7h` 之後的所有 commit 紀錄！*

   如果您先前已經把壞掉的版本 push 到 GitHub，在本地強制回退後，還需要強制推送到 GitHub 線上更新：
   ```bash
   # 強制推送到線上倉庫更新（這會覆蓋 GitHub 線上的歷史紀錄）
   git push origin main --force
   ```
