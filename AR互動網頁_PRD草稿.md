# AR 互動網頁產品需求文件草稿

版本：v0.2  
日期：2026-05-23  
狀態：已依需求回覆更新，待確認細節

## 1. 產品概念

建立一個可由手機瀏覽器開啟的 AR 互動網頁。管理者可在後台上傳印刷圖片與對應的 2D 動畫影片。使用者進入網頁後，先看到已上架的圖片清單。當使用者點擊其中一張圖片後，網頁會請求開啟相機。若相機畫面辨識到使用者剛剛點選的那張印刷圖片，畫面上會以矩形影片的形式疊加播放對應的 2D 動畫。

## 2. 可行性評估

### 2.1 結論

此產品可以實現。建議第一版採用「影像追蹤式 WebAR」製作，而不是直接使用 WebXR 作為核心方案。

原因如下：

- 需求本質是「辨識指定圖片並在圖片位置上播放動畫」，這正是 image tracking / natural feature tracking 的典型應用。
- iPhone Safari 目前仍不適合作為穩定 WebXR AR 方案，因此若希望 iOS 與 Android 都能用，一開始應選擇可直接透過相機影像做圖片辨識的 WebAR 函式庫。
- 若未來需要大量圖片、高穩定度、商業級追蹤效果或曲面包裝追蹤，則可評估 8th Wall 等付費 SDK。
- 本專案開發需以完全免費為原則，第一版不使用付費 SDK、付費雲端服務或付費素材服務。

### 2.2 推薦技術方向

#### 方案 A：開源 WebAR，適合 MVP

建議使用：

- MindAR 或 AR.js 做圖片辨識與追蹤
- Three.js 或 A-Frame 顯示動畫
- 前端框架製作圖片列表、掃描頁與後台管理介面
- 後端服務處理登入、素材上傳、資料儲存與發布
- 第一版先使用專案本機檔案保存圖片、辨識資料與影片
- 若要公開上線，優先評估免費額度平台或自有免費主機；若平台條件不足，再另行討論

優點：

- 不需要安裝 App
- 可快速做出可測試原型
- 成本低
- 適合先驗證互動流程、圖片辨識效果與動畫呈現
- 符合完全免費開發的要求

限制：

- 圖片品質會大幅影響辨識穩定度
- 大量圖片會增加載入時間與手機效能壓力
- 追蹤穩定度通常不如原生 App 或商業 SDK
- 對光線、反光、模糊、拍攝角度較敏感
- 免費部署平台可能有流量、檔案大小、儲存空間或伺服器執行時間限制

#### 方案 B：商業 WebAR SDK，適合正式商用

可評估 8th Wall 等商業服務。

此方案不納入第一版，因為目前需求明確要求完全免費開發。

優點：

- 圖片追蹤穩定度與跨裝置相容性通常較佳
- 支援較大型的商業活動與品牌體驗
- 後續可擴充世界追蹤、互動 3D、分析數據等能力

限制：

- 有授權費用
- 需要依照平台工作流程管理專案與素材

### 2.3 不建議作為第一版主軸的方向

不建議第一版直接以 WebXR 作為核心，因為 iOS Safari 對 WebXR AR 的支援仍不是穩定通用方案。若目標使用者包含 iPhone，WebXR 會造成體驗落差。

## 3. 目標使用者

第一版假設有兩類使用者：

- 一般使用者：使用 iPhone 或 Android 手機，透過連結或 QR Code 開啟網頁並體驗 AR。
- 管理者：透過後台上傳 3 張印刷圖片與各自對應的 2D 動畫影片，並管理是否上架。

可能使用情境：

- 展覽導覽：點選展品圖片後掃描現場圖卡，出現動畫解說
- 教材互動：掃描課本插圖，出現補充動畫
- 品牌活動：掃描海報、明信片、包裝，播放角色或商品動畫
- 收藏卡互動：掃描卡面，顯示卡片角色動畫

## 4. 核心使用流程

1. 使用者開啟網頁。
2. 首頁顯示可選擇的圖片清單。
3. 使用者點擊其中一張圖片。
4. 系統進入掃描頁，請求相機權限。
5. 使用者允許相機權限。
6. 系統顯示即時相機畫面與掃描提示。
7. 當相機辨識到所選圖片時，在圖片位置上疊加播放對應動畫。
8. 若圖片離開鏡頭，動畫暫停、淡出或顯示重新掃描提示。
9. 使用者可返回圖片清單，選擇另一張圖片。

管理者流程：

1. 管理者登入後台。
2. 新增 AR 項目。
3. 上傳印刷圖片。
4. 上傳對應的 2D 動畫影片。
5. 系統產生或更新圖片辨識資料。
6. 管理者填寫名稱、排序與顯示設定。
7. 管理者發布項目。
8. 使用者首頁顯示已發布的圖片。

## 5. 第一版功能需求

### 5.1 圖片清單

- 顯示後台已發布的圖片縮圖
- 每張圖片需有名稱或簡短標籤
- 點擊圖片後進入該圖片的 AR 掃描模式
- 支援返回首頁

### 5.2 相機與權限

- 點擊圖片後才請求相機權限
- 若使用者拒絕權限，顯示可理解的提示
- 支援 iOS Safari、Android Chrome 的基本相機開啟流程
- 必須使用 HTTPS 部署，否則多數手機瀏覽器不會允許相機功能

### 5.3 圖片辨識

- 每張可掃描圖片需先轉換成辨識資料
- 系統只辨識使用者當下點選的圖片，以降低載入量與辨識壓力
- 圖片應避免大面積純色、重複圖案、低解析度、強反光或過度模糊
- 建議原始圖片至少 1000px 寬，印刷或顯示版本需保持清晰
- 第一版圖片來源為印刷品，測試時需使用實際印刷尺寸與紙材

### 5.4 動畫呈現

第一版動畫類型確定為一般矩形 2D 影片，不需要透明背景。

建議支援：

- MP4：作為第一版基本格式，相容性最佳
- WebM：暫不作為第一版必要格式

顯示方式：

- 動畫疊在被掃描圖片上方
- 動畫可預設貼滿或覆蓋在圖片平面上
- 動畫尺寸可依圖片比例設定，必要時後台可調整縮放與位置
- 可設定自動播放、循環播放、偵測到圖片後重新播放
- 可設定圖片消失後暫停或隱藏
- 手機瀏覽器限制下，影片預設需 muted、playsinline，以提高自動播放成功率

### 5.5 素材管理

每個 AR 項目至少包含：

- 顯示用縮圖
- 辨識用圖片
- 圖片追蹤資料
- 對應動畫檔案
- 顯示比例、位置、旋轉、縮放設定

第一版需要後台素材管理功能。後台需支援新增、編輯、刪除、排序、發布與下架 AR 項目。

### 5.6 後台管理

後台第一版需求：

- 管理者登入
- 新增 AR 項目
- 上傳印刷圖片
- 上傳 2D 動畫影片
- 設定圖片名稱
- 設定項目是否發布
- 調整顯示順序
- 預覽縮圖
- 刪除或下架項目

系統處理：

- 圖片上傳後需建立可供 AR 辨識使用的目標資料
- 影片上傳後需保存原始檔案，必要時產生壓縮版本
- 前台只顯示已發布項目
- 掃描頁只載入使用者點選項目的辨識資料與影片

## 6. 非功能需求

### 6.1 裝置與瀏覽器

第一版目標：

- iPhone：Safari 最新版
- Android：Chrome 最新版

需要實機測試：

- iPhone 至少 2 款不同世代
- Android 至少 2 款不同品牌或效能等級

### 6.2 效能

- 首頁需快速載入，不應一次下載所有大型動畫
- 點擊圖片後，只載入該圖片的辨識資料與動畫
- 掃描頁需有載入狀態，避免使用者以為壞掉
- 動畫檔案需壓縮，避免手機網路等待過久
- 後台上傳時應限制檔案大小，避免單一素材拖慢前台體驗

### 6.3 隱私

- 相機畫面只在使用者裝置上即時處理
- 第一版不儲存、不上傳相機影像
- 權限說明需清楚告知相機用途

### 6.4 無障礙與錯誤處理

- 若相機不可用，提供錯誤提示
- 若辨識太久沒有成功，提供「靠近圖片」、「保持光線充足」、「避免反光」等提示
- 提供返回圖片清單的清楚入口

## 7. MVP 範圍

第一版做小而完整的可測試版本：

- 3 張印刷圖片
- 每張圖片對應 1 個動畫
- 手機瀏覽器可開啟
- 可選圖、開相機、掃描、播放動畫、返回
- 後台可上傳圖片與 2D 影片
- 後台可發布或下架項目
- 完成 iPhone 與 Android 實機測試

暫不包含：

- 會員系統
- 複雜互動遊戲
- 多張圖片同時辨識
- 大量圖片一次載入
- 使用者錄影或分享功能
- 多管理員權限分級
- 自動剪輯或製作影片

## 8. 後續可擴充功能

- 多管理員權限
- 更完整的後台素材分類
- 多圖片同時辨識
- 掃描成功後解鎖內容
- 按鈕、音效、點擊互動
- 拍照或錄影分享
- 活動數據分析
- 多語系
- 離線快取
- GPS 或 QR Code 分區活動

## 9. 主要風險

### 9.1 圖片辨識不穩

風險：圖片特徵不足、反光、角度太斜、光線太暗，可能導致辨識失敗或動畫抖動。  
對策：先用實際圖片做追蹤測試，建立圖片選擇規範。

### 9.2 iOS 相容性

風險：iOS 瀏覽器相機權限、影片自動播放、透明影片格式支援可能有差異。  
對策：MVP 一開始就納入 iPhone 實機測試。

### 9.3 素材檔案過大

風險：動畫太大會造成載入慢、手機發熱或掃描不順。  
對策：限制單一動畫大小，使用延遲載入與壓縮格式。

### 9.4 圖片數量過多

風險：若一次支援數十或數百張圖片，辨識資料與效能會變成核心問題。  
對策：第一版只載入使用者選中的圖片；大量圖片需求再評估商業 SDK 或分批載入策略。

### 9.5 後台自動產生辨識資料的穩定性

風險：不同 WebAR 技術對圖片目標資料的產生方式不同，有些工具較適合開發階段手動轉換，有些需要自建處理流程。  
對策：第一版開發時優先選擇能整合自動產生目標資料的技術；若工具限制較多，先讓後台保存素材，由管理端流程觸發轉換。

## 10. 素材規格初稿

### 10.1 辨識圖片

- 格式：JPG 或 PNG
- 建議解析度：至少 1000px 寬
- 圖像特性：高對比、細節豐富、不重複、不大面積純色
- 避免：反光材質、過度對稱、重複花紋、低解析度截圖
- 第一版預期印刷尺寸：明信片到 A4
- 印刷尺寸不會阻止使用者掃描，使用者可以透過靠近或遠離調整畫面大小；但尺寸會影響建議掃描距離、追蹤穩定度測試與影片預設疊加比例

### 10.2 動畫

第一版支援格式：

- MP4：優先支援，建議 H.264 編碼
- WebM：非必要格式，可作為後續擴充

建議影片規格：

- 長度：建議 3 到 15 秒
- 檔案大小：建議單支壓縮後 5MB 以內，視畫質需求調整
- 比例：依印刷圖片比例或預期疊加區域設定
- 聲音：第一版建議預設無聲或靜音播放，避免手機瀏覽器阻擋自動播放

## 11. 成功指標

MVP 成功條件：

- 使用者能在 30 秒內理解流程並進入掃描
- 圖片在正常光線下 2 秒內可被辨識
- 動畫能穩定貼在圖片上，沒有嚴重偏移
- iPhone 與 Android 都可完成完整流程
- 首頁載入與掃描頁載入等待時間可接受
- 管理者能從後台完成新增、上傳、發布、前台顯示的完整流程

## 12. 已確認需求

- 圖片來源：印刷品
- 印刷尺寸：明信片到 A4
- 第一版圖片數量：3 張
- 動畫類型：一般矩形 2D 影片，不需要透明背景
- 使用流程：使用者先點選圖片，再開啟相機掃描該圖片
- 素材管理：需要後台讓管理者上傳圖片與動畫

## 13. 待確認細節

1. 網站是否需要公開上線，還是先在本機或測試網址驗證？
2. 是否需要前台品牌視覺，例如 Logo、主色、標題文案？

## 14. 建議下一步

先製作一個 MVP 原型：

- 建立前台圖片清單與掃描頁
- 建立後台素材上傳與發布流程
- 放入 3 張印刷圖片與 3 支 2D 影片
- 用開源 WebAR 技術完成手機版流程
- 在 iPhone 與 Android 實機測試辨識穩定度
- 根據測試結果決定是否繼續用開源方案，或改評估商業 SDK

## 15. 參考來源

- AR.js Image Tracking 文件：https://ar-js-org.github.io/AR.js-Docs/image-tracking/
- AR.js NFT Marker Creator：https://ar-js-org.github.io/NFT-Marker-Creator/
- 8th Wall Image Targets：https://www.8thwall.com/products/image-targets
- WebXR 支援狀態參考：https://www.webxr.fyi/
